(function ($, root, undefined) {

	$(function () {

        'use strict';

        // DOM ready, take it away
        /* Nivo-Slider */
        $(window).load(function() {
            $('#slider').nivoSlider();
        });

	});

})(jQuery, this);
